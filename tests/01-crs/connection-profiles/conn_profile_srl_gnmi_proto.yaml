apiVersion: inv.sdcio.dev/v1alpha1
kind: TargetConnectionProfile
metadata:
  name: test-srl-gnmi-proto
  namespace: default
  labels:
    scdio.dev/env: ci-test
spec:
  port: 57400
  protocol: gnmi
  encoding: PROTO
  skipVerify: true
  insecure: false