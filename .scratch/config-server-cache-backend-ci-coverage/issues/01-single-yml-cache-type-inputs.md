# 01 — `single.yml`: `cache_type` + suite-selection inputs

**What to build:** Extend the reusable `single.yml` workflow with two new optional inputs so a caller can deploy `data-server` with the alternate cache backend and choose a reduced set of test suites to run:

- `cache_type` (default `local`): when set to `config-server`, the existing "Set the versions to test" step's `yq`-patch mechanism (the same one already patching in `DEVIATION_INTERVAL` and the trace-level flag) additionally patches `cache.type: config-server` and `cache.address: 127.0.0.1:56010` into the generated `data-server` ConfigMap. When left at `local`, the generated ConfigMap is unchanged from today's behavior.
- A suite-selection input controlling which explicit suite steps (`00`–`05`) run. Suites `06+` remain auto-discovered. Default includes `04-sensitive`; `05-cache-backend` is selected only by the cache-backend job. Workflow validation rejects `05-cache-backend` unless `cache_type: config-server`.

Default behavior (both inputs at their defaults) must be unchanged from today — `cicd.yml` and `matrix-cicd.yml` continue to work without modification until ticket 03 wires in the new job.

**Blocked by:** None — can start immediately.

**Status:** in-progress (code complete; final CI-dispatch verification below still needs a maintainer with GH Actions dispatch access)

- [x] `single.yml` accepts a `cache_type` input (default `local`) and, when set to `config-server`, patches `cache.type`/`cache.address` into `./config-server/artifacts/out/artifacts.yaml` alongside the existing `DEVIATION_INTERVAL`/trace-level patches
- [x] `single.yml` accepts a suite-selection input (`suites_to_run`) that gates explicit steps for suites `00`–`05`; auto-discovery starts at `06+`
- [x] Workflow validation rejects `05-cache-backend` unless `cache_type: config-server` (reverse direction of the existing config-server + 02/03/04 guard)
- [x] Leaving both new inputs at their defaults reproduces today's exact behavior — verified locally against `config-server`'s generated `artifacts/out/artifacts.yaml` (built via `make artifacts` on the `config-server-cache-backend` branch at `/home/mava/projects/config-server`): with `cache_type: local` the new `yq` patch's `if` guard never executes, and `suites_to_run` defaults to all four suite IDs so every `contains()` guard evaluates true, reproducing today's unconditional steps. **Now also confirmed in real CI**: `sdcio/data-server` PR #471's run [31498684771](https://github.com/sdcio/data-server/actions/runs/31498684771) (2026-08-18, triggered via the `Pairs-with: sdcio/integration-tests#115` chain, no `cache_type`/`suites_to_run` inputs passed) shows the "Set the versions to test" step only applying the two pre-existing `yq` patches (`DEVIATION_INTERVAL`, trace flag) — the new cache-backend patch's `if` guard correctly did not fire — and all of `00-setup`/`01-crs`/`02-crud`/`03-deviations`/`04-sensitive` passed exactly as before this change.
- [ ] A manual/temporary dispatch with `cache_type: config-server` and a reduced suite list (e.g. `00-setup`,`01-crs` only) completes a full cluster bring-up and passes those two suites, with the generated ConfigMap demonstrably carrying `cache.type: config-server` / `cache.address: 127.0.0.1:56010` — **still not verified**. What *was* verified: (a) locally, the `yq` patch expression correctly rewrites the embedded `data-server.yaml` ConfigMap string to `cache.type: config-server` / `cache.address: 127.0.0.1:56010` (via `from_yaml`/`to_yaml`) without disturbing the existing patches; (b) in real CI (above), that the patch's `if` guard is correctly skipped when `cache_type` is left at its default. What's still missing is an actual run *with* `cache_type: config-server` set — `sdcio/data-server`#471's own `cicd.yml` never passes that input (it only resolves `integration_tests_ref`/`configserver_version` via `Pairs-with`), so this needs either a direct `workflow_dispatch` of `single.yml`, or ticket 03's dedicated job in `integration-tests`' own `cicd.yml`.
- [x] No changes to `config-server`'s `configmap-input-vars.yaml.tmpl` or any other kform input (per ADR 0002, this stays a CI-side `yq` patch) — confirmed no files under `/home/mava/projects/config-server` were touched

**Review notes (code-review pass, `/code-review`):** Standards review flagged that nothing in the workflow enforced `CONTEXT.md`'s "Cache-backend suite" invariant — "never against the same deployment as `02-crud`/`03-deviations`, since `Cache.Type` is fixed at deploy time" — a misconfigured caller could pass `cache_type: config-server` together with `02-crud`/`03-deviations` in `suites_to_run`. Fixed with a new fail-fast "Validate cache_type / suites_to_run combination" step that errors before any cluster is deployed if that combination is requested. The review also flagged that the original `contains(inputs.suites_to_run, 'XX-yyy')` guards did unanchored substring matching (a future suite name that's a substring of another, e.g. a hypothetical `02-crud-v2`, could false-positive-match `02-crud`); all six guards now use comma-delimited matching (`contains(format(',{0},', inputs.suites_to_run), ',02-crud,')`) to make matching exact-token. Spec review found no missing/partial requirements, no scope creep, and no functional defects within ticket 01's scope.
