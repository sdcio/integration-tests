apiVersion: v1
kind: ConfigMap
metadata:
  name: context
data: 
  configServerImage: registry.k8s.sdcio.dev/iptecharch/config-server:${CONFIG_VERSION}
  dataServerImage: registry.k8s.sdcio.dev/iptecharch/data-server:${DATA_VERSION}
