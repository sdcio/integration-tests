project_name: data-server
builds:
    goos:
      - linux
    goarch:
      - amd64
dockers:
  - goos: linux
    goarch: amd64
    ids:
      - data-server
    image_templates:
      - "ghcr.io/iptecharch/test-image:{{ .Version }}"
    dockerfile: Dockerfile
    skip_push: false
  - goos: linux
    goarch: amd64
    ids:
      - data-server
    image_templates:
      - "registry.k8s.hans.io/iptecharch/data-server:{{ .Version }}"
    dockerfile: Dockerfile
    skip_push: false
    push_flags:
      - --tls-verify=false
